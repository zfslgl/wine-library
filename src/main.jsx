import React from 'react'
import ReactDOM from 'react-dom/client'
import WineLibrary from './WineLibrary'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WineLibrary />
  </React.StrictMode>,
)
